<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="hr_job_view_form_inherit" model="ir.ui.view">
        <field name="name">hr.job.form.inherit</field>
        <field name="model">hr.job</field>
        <field name="inherit_id" ref="hr_recruitment.hr_job_survey"/>
        <field name="arch" type="xml">
            <xpath expr="//group[@name='recruitment']" position="inside">
                <field name="application_deadline"/>
                <field name="is_deadline_passed" invisible="1"/>
            </xpath>
        </field>
    </record>

    <record id="view_job_filter_deadline" model="ir.ui.view">
        <field name="name">hr.job.filter.deadline</field>
        <field name="model">hr.job</field>
        <field name="inherit_id" ref="hr.view_job_filter"/>
        <field name="arch" type="xml">
            <search position="inside">
                <field name="application_deadline"/>
                <separator/>
                <filter string="Deadline Passed" name="deadline_passed" 
                        domain="[('is_deadline_passed', '=', True)]"/>
                <filter string="Open for Applications" name="open_applications" 
                        domain="['|', ('application_deadline', '=', False), ('is_deadline_passed', '=', False)]"/>
                <group expand="0" string="Group By">
                    <filter string="Application Deadline" name="group_deadline" 
                            domain="[]" context="{'group_by':'application_deadline'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- Modify the existing action to include tree view -->
    <record id="hr_recruitment.action_hr_job" model="ir.actions.act_window">
        <field name="view_mode">kanban,tree,form</field>
    </record>

    <!-- Add archive filter -->
    <!-- <record id="view_hr_job_search_inherit" model="ir.ui.view">
        <field name="name">hr.job.search.inherit</field>
        <field name="model">hr.job</field>
        <field name="inherit_id" ref="hr_recruitment.view_job_filter"/>
        <field name="arch" type="xml">
            <search>
                <filter string="Archived" name="inactive" domain="[('active', '=', False)]"/>
            </search>
        </field>
    </record> -->

    <!-- Add action server for archiving first double check with the enterprise version -->
    <!-- <record id="action_archive_job" model="ir.actions.server">
        <field name="name">Archive/Unarchive</field>
        <field name="model_id" ref="hr.model_hr_job"/>
        <field name="binding_model_id" ref="hr.model_hr_job"/>
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code">
            records.toggle_active()
        </field>
    </record> -->
</odoo> 